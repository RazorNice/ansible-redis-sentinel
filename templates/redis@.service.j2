[Unit]
Description=Redis instance %i
After=network.target

[Service]
Type=simple
User=redis
Group=redis
ExecStart=/opt/redis/bin/redis-server /etc/redis/redis-%i.conf
PIDFile=/run/redis-%i/redis-%i.pid
RuntimeDirectory=redis-%i

Restart=on-failure

[Install]
WantedBy=multi-user.target
